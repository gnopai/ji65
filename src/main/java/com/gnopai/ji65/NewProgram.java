package com.gnopai.ji65;

import lombok.Value;

import java.util.List;

// TODO rename once we can replace the old "Program"
@Value
public class NewProgram {
    List<Byte> bytes;
    // TODO add labels here as indexes into the data
}
